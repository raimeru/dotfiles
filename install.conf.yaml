- defaults:
    link:
      relink: true
      create: true

- clean: ['~']

- link:
    ~/.config/nvim:
      path: config/nvim

- link:
    ~/.config/wezterm:
      path: config/wezterm

- shell:
  - [git submodule update --init --recursive, Installing submodules]
