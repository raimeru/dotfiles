- defaults:
    link:
      relink: true
      create: true

- clean: ['~']

- link:
    ~/.config/nvim:
      path: config/nvim

- shell:
  - [git submodule update --init --recursive, Installing submodules]
